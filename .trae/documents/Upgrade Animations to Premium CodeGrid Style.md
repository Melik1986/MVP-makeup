Я уточнил план на основе ваших правок. Мы сохраняем текущую анимацию заголовка (хаотичные буквы), применяем маски только к длинному тексту, а короткий текст и теги заставляем "влетать" с разных сторон. Для моделей добавляем эффект "перетекания" (Parallax Float).

### 📋 Уточненный план реализации

#### 1. Компоненты UI (Новые)

*   `src/shared/ui/animation/TextReveal.vue`:
    *   **Назначение**: Для длинного текста (Наставничество, Заработок).
    *   **Эффект**: Текст выезжает снизу вверх из-под маски (`overflow: hidden`).

*   `src/shared/ui/animation/MotionReveal.vue`:
    *   **Назначение**: Для короткого текста и тегов.
    *   **Свойства**: `direction` ('top', 'left', 'right', 'bottom').
    *   **Эффект**: Элемент "влетает" со смещением и фейдом (Fly In).

#### 2. Рефакторинг `HeroSection.vue`

*   **Заголовок ("ВИЗАЖИСТ")**: Оставляем **без изменений** (используем текущую верстку с `ref` для букв).
*   **Длинный текст**: Оборачиваем в `<TextReveal>`.
*   **Короткий текст (теги, подписи)**: Оборачиваем в `<MotionReveal direction="...">`.

#### 3. Апгрейд `useHeroAnimation.js` (GSAP Logic)

Мы перепишем таймлайн, используя метки (Labels), чтобы синхронизировать потоки:

*   **Изинг**: `CustomEase` ("snappy" curve).
*   **Start**:
    *   **Model 1**: Появление через `clip-path` (шторка снизу вверх).
    *   **Tags (Top)**: Влетают сверху (`y: -50 -> 0`).
*   **Sequence**:
    *   **Left Text**: Влетает слева (`x: -50 -> 0`).
    *   **Right Text**: Влетает справа (`x: 50 -> 0`).
*   **Model Transitions (Parallax Flow)**:
    *   **1 -> 2**: Model 1 уплывает вправо (`x: 0 -> 50`, `opacity: 0`), Model 2 выплывает слева (`x: -50 -> 0`, `opacity: 1`). Это создаст ощущение движения конвейера/карусели.
    *   **2 -> 3**: Аналогичный сдвиг вправо.

#### 4. Технические детали
*   Установим `gsap` CustomEase (если доступен в проекте, иначе используем `power4.out` или `cubic-bezier`).
*   Гарантируем отсутствие "скачков" через `gsap.set` начальных позиций.

Подтверждаете этот сценарий?
